@{
    var prescriptionRecordsCount = ViewData["PrescriptionRecordsCount"] ?? 0;
    var medicationRecordsCount = ViewData["MedicationRecordsCount"] ?? 0;

    var pharmacistName = ViewData["PharmacistName"] as string ?? "Pharmacist";
    var pharmacistSurname = ViewData["PharmacistSurname"] as string ?? "Name";
}



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Surgeon Dashboard</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Mont+Blanc:wght@600&display=swap" rel="stylesheet">


    <!-- Bootstrap Icons CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.8.1/font/bootstrap-icons.min.css">


    <style>
        .dashboard-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            font-family: 'Mont Blanc', sans-serif;
            font-weight: 600;
            color: white;
            margin-bottom: 40px;
        }

        .header-content {
            display: block;
            align-items: center;
        }

        .dashboard-icon {
            margin-right: 15px;
            height: 50px; /* Adjust size as needed */
        }

        .dashboard-title {
            font-size: 1.5rem; /* Adjust size as needed */
            margin: 0;
        }

        .greeting {
            font-size: 1.25rem; /* Slightly smaller font for the greeting */
            font-weight: 500;
            margin-right: 8px; /* Adjust space between greeting and name */
            color: #fff; /* Match text color with the rest of the dashboard */
        }

        .datetime-container {
            display: flex;
            flex-direction: column;
            align-items: flex-end;
            color: #fff;
            font-family: 'Mont Blanc', sans-serif;
        }

            .datetime-container .time {
                font-size: 1.5rem;
                font-weight: bold;
            }

            .datetime-container .date {
                font-size: 1rem;
                margin-top: 5px;
            }

        .dashboard-header h1 {
            font-size: 1.5rem; /* Smaller heading size */
            margin: 0;
        }

        body {
            background-image: url('/images/pharmacy dashboard.jpg');
            background-size: cover;
            background-position: center;
            font-family: Arial, sans-serif;
            color: #333;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        .card {
            border: none;
            border-radius: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
            text-align: center;
        }

            .card:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            }

        .card-icon {
            font-size: 3rem;
            color: #007bff;
            margin-top: 20px;
        }

        .card-content {
            padding: 20px;
        }

        .card-title {
            font-size: 1.5rem;
            margin-bottom: 10px;
        }

        .card-description {
            color: #6c757d;
        }

        .container {
            margin-top: 40px;
        }

        .row {
            display: flex;
            justify-content: center;
        }

        .badge-count {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: #007bff;
            color: #fff;
            border-radius: 50%;
            width: 24px;
            height: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
        }

        .nav-link {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100%;
            text-decoration: none;
        }

        .modal-body {
            padding: 20px;
        }


        .pharmacist-dashboard-text {
            font-size: 1.2em; /* Adjust size as needed */
            color: #fff; /* Change the color as needed */
            margin-top: 0.5em; /* Space between greeting and the text */
            /* margin-bottom: 1em; */ /* Space below the text */
        }

    </style>
</head>
<body>
    <div class="container">
        <div class="dashboard-header">
            <div class="header-content">
                <img src="~/images/PharmacistDashboard.png" alt="Surgeon Dashboard" class="dashboard-icon">
                <h1 class="dashboard-title">
                    <span id="greeting" class="greeting"></span>, Dr. @pharmacistName @pharmacistSurname
                </h1>
                <h2 class="pharmacist-dashboard-text">Pharmacist Dashboard:</h2>
            </div>
            <div class="datetime-container">
                <div class="time" id="time"></div>
                <div class="date" id="date"></div>
                <div id="weather" class="mt-2" style="font-size: 1rem; color: #fff;"></div>
                <button class="btn btn-primary mt-2" data-bs-toggle="modal" data-bs-target="#calendarModal">
                    <i class="bi bi-calendar"></i> Open Calendar
                </button>
            </div>
        </div>
        <div class="row">
            <div class="col-md-4 mb-4">
                <div class="card">
                    <!-- Anchor tag to make the card clickable -->
                    <a href="@Url.Action("Index", "MedicationRecords")" class="text-decoration-none text-dark">
                        <div class="badge-count">@medicationRecordsCount</div> <!-- Dynamic count -->
                        <div class="card-body">
                            <div class="card-icon">
                                <i class="bi bi-box"></i>
                            </div>
                            <div class="card-content">
                                <h5 class="card-title">Inventory</h5>
                                <p class="card-description">Manage and view your inventory of medications.</p>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card">
                    <!-- Anchor tag to make the card clickable -->
                    <a href="@Url.Action("Index", "PrescriptionRecords")" class="text-decoration-none text-dark">
                        <div class="badge-count">@prescriptionRecordsCount</div> <!-- Dynamic count -->
                        <div class="card-body">
                            <div class="card-icon">
                                <i class="bi bi-file-medical"></i>
                            </div>
                            <div class="card-content">
                                <h5 class="card-title">Prescriptions</h5>
                                <p class="card-description">View and manage prescriptions here.</p>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
            <!-- Reports Card -->
            <div class="col-md-4 mb-4">
                <div class="card">
                    <!-- Anchor tag to make the card clickable -->
                    <a href="@Url.Action("Report", "DispensedPrescriptions")" class="text-decoration-none text-dark">
                        <div class="card-body">
                            <div class="card-icon">
                                <i class="bi bi-bar-chart"></i>
                            </div>
                            <div class="card-content">
                                <h5 class="card-title">Reports</h5>
                                <p class="card-description">Generate and review various system reports.</p>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card">
                    <a href="@Url.Action("Reports", "MedicationRecords")" class="text-decoration-none text-dark">
                        <div class="card-body">
                            <div class="card-icon">
                                <i class="fa-solid fa-chart-pie"></i>
                            </div>
                            <div class="card-content">
                                <h5 class="card-title">Analytics</h5>
                                <p class="card-description">Manage and review analytics.</p>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card">
                    <a href="@Url.Action("Index", "OrderItems")" class="text-decoration-none text-dark">
                    <div class="card-body">
                        <div class="card-icon">
                                <i class="fa-solid fa-truck-fast"></i>
                        </div>
                        <div class="card-content">
                            <h5 class="card-title">Medication Orders</h5>
                            <p class="card-description">Access data of ordered medications.</p>
                        </div>
                    </div>
                    </a>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card">
                    <a class="nav-link text-dark d-flex align-items-center"
                       asp-area="Identity"
                       asp-page="/Account/Manage/Index"
                       title="Manage">
                        <div class="card-body">
                            <div class="card-icon">
                                <i class="bi bi-gear"></i>
                            </div>
                            <div class="card-content">
                                <h5 class="card-title">Settings</h5>
                                <p class="card-description">Update your personal and system settings here.</p>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <style>
        .modal-dialog-centered {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            border-radius: 10px; /* Optional: rounded corners */
        }
    </style>

    <!-- Calendar Modal -->
    <div class="modal fade" id="calendarModal" tabindex="-1" aria-labelledby="calendarModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-md">
            <!-- Changed to 'modal-md' for medium size -->
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="calendarModalLabel">Calendar</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <!-- Embed or add calendar here -->
                    <iframe src="https://calendar.google.com/calendar/embed?src=your_calendar_id" style="border: 0; width: 100%; height: 400px;" frameborder="0" scrolling="no"></iframe>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>


    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.min.js"></script>

    <script>
        function updateDateTime() {
            const now = new Date();
            document.getElementById('time').textContent = now.toLocaleTimeString();
            document.getElementById('date').textContent = now.toLocaleDateString();
        }

        setInterval(updateDateTime, 1000); // Update time every second
        updateDateTime(); // Initialize immediately
    </script>

    <script>
        async function fetchWeather() {
            const apiKey = 'd3c5b0078ead4cecb51161433241909 ';
            const city = 'Port Elizabeth';  // Set the city based on your location
            const url = `https://api.weatherapi.com/v1/current.json?key=${apiKey}&q=${city}&aqi=no`;

            try {
                const response = await fetch(url);
                const weatherData = await response.json();
                const temperature = weatherData.current.temp_c;
                const description = weatherData.current.condition.text;
                const icon = weatherData.current.condition.icon;

                document.getElementById('weather').innerHTML = `
                                                <div>
                                            <img src="${icon}" alt="Weather Icon" style="width:50px; height:50px;" />
                                            <span style="font-size: 1.2rem;">${temperature}°C, ${description}</span>
                                        </div>
                                    `;
            } catch (error) {
                console.error('Error fetching weather data:', error);
                document.getElementById('weather').innerText = 'Weather info unavailable';
            }
        }

        window.onload = function () {
            fetchWeather();
            updateTime();
        };

        function updateTime() {
            const now = new Date();
            const time = now.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
            const date = now.toLocaleDateString();

            document.getElementById('time').innerText = time;
            document.getElementById('date').innerText = date;

            setTimeout(updateTime, 60000); // Update every minute
        }
    </script>
    <script>
        $(document).ready(function () {
            const now = new Date();
            const currentHour = now.getHours();
            let greeting;

            if (currentHour < 12) {
                greeting = "Good Morning";
            } else if (currentHour >= 12 && currentHour < 18) {
                greeting = "Good Afternoon";
            } else {
                greeting = "Good Evening";
            }

            // Add greeting dynamically to the title
            document.getElementById("greeting").textContent = greeting;
        });

    </script>

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
</body>
</html>


