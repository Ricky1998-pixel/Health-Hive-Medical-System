@* model IEnumerable<Health_Hive_Project_2024.Models.ConditionDiagnosisRecords>

    @{
        ViewData["Title"] = "Index";
    }

    <h1>Index</h1>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Patient)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Condition)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Patient.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Condition.Diagnosis)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.DiagnosisID">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.DiagnosisID">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.DiagnosisID">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
 *@
