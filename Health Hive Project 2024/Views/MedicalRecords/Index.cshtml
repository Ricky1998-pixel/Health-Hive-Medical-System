@model IEnumerable<Health_Hive_Project_2024.Models.MedicalRecords>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Allergies)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurrentMedication)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            
            <td>
                @Html.DisplayFor(modelItem => item.CurrentMedication)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RecordsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RecordsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RecordsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
