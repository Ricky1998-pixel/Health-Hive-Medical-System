@model IEnumerable<Health_Hive_Project_2024.Models.MedicationAdministration>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Medication)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nurse)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Patient)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Medication.MedicationName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nurse.ContactNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Patient.Address)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AdministrationID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AdministrationID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AdministrationID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
